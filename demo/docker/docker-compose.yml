version: '3.7'

services:
  influxdb:
    container_name: nussknacker_influxdb
    image: influxdb:${INFLUXDB_VERSION}
    command: ["/runWithEspDb.sh"]
    volumes:
      - ./influxdb/influxdb.conf:/etc/influxdb/influxdb.conf
      - nussknacker_storage_influxdb:/var/lib/influxdb/data
      - ./influxdb/runWithEspDb.sh:/runWithEspDb.sh
    expose:
      - "2003/udp"

  zookeeper:
    container_name: nussknacker_zookeeper
    image: zookeeper:${ZOOKEEPER_VERSION}
    expose:
      - "2181"

  kafka:
    container_name: nussknacker_kafka
    image: ${KAFKA_REPOSITORY}:${KAFKA_VERSION}
    expose:
      - "9092"
    environment:
      KAFKA_ADVERTISED_HOST_NAME: kafka
      KAFKA_ADVERTISED_PORT: 9092
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_HEAP_OPTS: -Xms128m -Xmx512m
    links:
      - zookeeper
    depends_on:
      - zookeeper

  jobmanager:
    container_name: nussknacker_jobmanager
    image: flink:${FLINK_VERSION}
    expose:
      - "6123"
      - "8081"
    entrypoint: /flink-entrypoint.sh
    command: jobmanager
    depends_on:
      - zookeeper
      - kafka
    environment:
      - JOB_MANAGER_RPC_ADDRESS=jobmanager
    links:
      - influxdb
      - zookeeper
    volumes:
      - ./flink/lib/flink-queryable-state-runtime_2.12-1.7.2.jar:/opt/flink/lib/flink-queryable-state-runtime_2.12-1.7.2.jar
      - ./flink/lib/flink-metrics-graphite-1.7.2.jar:/opt/flink/lib/flink-metrics-graphite-1.7.2.jar
      - ./flink/flink-conf.yaml:/opt/flink/conf/flink-conf.yaml
      - ./flink/flink-entrypoint.sh:/flink-entrypoint.sh
      - nussknacker_storage_jobmanager:/opt/flinkData/

  taskmanager:
    container_name: nussknacker_taskmanager
    image: flink:${FLINK_VERSION}
    entrypoint: /flink-entrypoint.sh
    command: taskmanager
    links:
      - influxdb
      - jobmanager
      - kafka
      - zookeeper
    depends_on:
      - zookeeper
      - kafka
      - jobmanager
    environment:
      - JOB_MANAGER_RPC_ADDRESS=jobmanager
    volumes:
      - ./flink/lib/flink-queryable-state-runtime_2.12-1.7.2.jar:/opt/flink/lib/flink-queryable-state-runtime_2.12-1.7.2.jar
      - ./flink/lib/flink-metrics-graphite-1.7.2.jar:/opt/flink/lib/flink-metrics-graphite-1.7.2.jar
      - ./flink/flink-conf.yaml:/opt/flink/conf/flink-conf.yaml
      - ./flink/flink-entrypoint.sh:/flink-entrypoint.sh
      - nussknacker_storage_taskmanager:/opt/flinkData/
    ulimits:
      nproc: 70000
      nofile:
        soft: 70000
        hard: 70000

  elasticsearch:
    container_name: nussknacker_elasticsearch
    image: docker.elastic.co/elasticsearch/elasticsearch:${ELASTICSEARCH_VERSION}
    environment:
      ES_JAVA_OPTS: -Xms128m -Xmx512m
    volumes:
      - ./elasticsearch/config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml
    expose:
      - "9200"
      - "9300"
    ulimits:
      nproc: 70000
      nofile:
        soft: 70000
        hard: 70000

  logstash:
    container_name: nussknacker_logstash
    image: docker.elastic.co/logstash/logstash:${LOGSTASH_VERSION}
    links:
      - elasticsearch
      - kafka
    expose:
        - "5000"
    depends_on:
      - elasticsearch
      - kafka
    volumes:
      - ./logstash/config/logstash.yml:/usr/share/logstash/config/logstash.yml
      - ./logstash/pipeline:/usr/share/logstash/pipeline

  kibana:
    container_name: nussknacker_kibana
    image: docker.elastic.co/kibana/kibana:${KIBANA_VERSION}
    expose:
      - "5601"
    links:
      - elasticsearch
    depends_on:
      - elasticsearch
    volumes:
      - ./kibana/config/:/usr/share/kibana/config

  grafana:
    container_name: nussknacker_grafana
    image: grafana/grafana:5.1.5
    entrypoint: ["/runWithFlinkEspBoard.sh"]
    volumes:
      - ./grafana/runWithFlinkEspBoard.sh:/runWithFlinkEspBoard.sh
      - ./grafana/Flink-ESP.json:/Flink-ESP.json
    links:
      - "influxdb:influxdb"
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_SERVER_ROOT_URL=%(protocol)s://%(domain)s:/grafana
    depends_on:
      - influxdb

  app:
    container_name: nussknacker_app
    image: ${NUSSKNACKER_IMAGE}:${NUSSKNACKER_VERSION}
    entrypoint: ["/opt/nussknacker/bin/wait-for-it.sh"]
    command: ["-h", "taskmanager", "-p", "6126", "-t", "60", "--", "/opt/nussknacker/bin/docker-run.sh"]
    links:
      - taskmanager
      - jobmanager
      - kafka
      - grafana
      - zookeeper
    depends_on:
      - zookeeper
      - jobmanager
      - taskmanager
    expose:
      - "8080"
    environment:
      - NUSSKNACKER_CONFIG_FILE=/opt/nussknacker/conf/docker-compose-application.conf
    volumes:
      - nussknacker_storage_app:/opt/nussknacker/storage

  nginx:
    container_name: nussknacker_nginx
    image: nginx
    ports:
      - "8081:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - zookeeper
      - influxdb
      - kafka
      - jobmanager
      - taskmanager
      - elasticsearch
      - logstash
      - kibana
      - grafana
      - app

volumes:

  nussknacker_storage_taskmanager:

  nussknacker_storage_jobmanager:

  nussknacker_storage_influxdb:

  nussknacker_storage_app:
