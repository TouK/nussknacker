{
  include file("develConf/sample/application.conf")

  processTypes {
    streaming {
      modelConfig {
        plugins {
          literalExpressions {
            defaultValue: "Default for streaming"
          }
        }
        nodes: ${nodes}
      }
    }

    streaming2 {
      modelConfig {
        plugins {
          literalExpressions {
            defaultValue: "Default for streaming2"
          }
        }
        nodes: ${nodes}
      }
    }
  }

  nodes{
    multipleParamsService: {
      params {
        foo {
          language: "literal"
          defaultValue: ""
        }
      }
    }
  }
}