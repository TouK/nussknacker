// @flow
export type Edge = any;

export opaque type GroupId = string;
export type GroupType = {
  id: GroupId;
};

export opaque type NodeId = string;
export type NodeType = {
  id: NodeId;
};

export interface Process {
}