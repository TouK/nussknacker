import { styled } from "@mui/material";
import React from "react";
import { MonoNuIcon } from "../../common/nuIcon";

export const IconImg = styled(MonoNuIcon)({
    fontSize: "1.5rem",
    verticalAlign: "middle",
});
