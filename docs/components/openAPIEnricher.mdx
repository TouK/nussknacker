---

# front matter metadata for Docusaurus
title: OpenAPI Enricher
description: Enrich scenario with results of external services and perform actions in the external systems
sidebar_label: OpenAPI Enricher
# sidebar_position: 

# front matter metadata for LLM
componentName: OpenAPI Enricher
processingModes: [Streaming, Request-Response]
componentPaletteGroup: Enrichers
# keywords: []
---

import ModeStreaming from '/badges/modeStreaming.mdx';
import ModeRequestResponse from '/badges/modeRequestResponse.mdx';


# OpenAPI Enricher

<!-- Processing modes -->
<ModeStreaming /> <ModeRequestResponse />

## Description

OpenAPI Enricher is a particular type of [enricher](/docs/about/GLOSSARY.md#enricher) which uses Open API standard for inter-systems commmunication. It is autogenerated by Nussknacker based on the OpenAPI interface definition. You need to configure [OpenAPI integration](/docs/integrations/openAPI.md) to have this component available in the Component Palette. 

:::note
In this documentation, **record** is a neutral term used across all [processing modes](/docs/about/ProcessingModes.md).  
In purely streaming contexts, we typically use term **event** - that simply means a  record with a timestamp, which lets Nussknacker apply time-based processing logic.
:::

## Parameters and configuration

| Name                  | Description                                       |
|-----------------------|---------------------------------------------------|
| OperationId | Id of the OpenAPI operation, as defined in the interface definition |
| Input parameters | An entry field for each of the operation's input parameters. Nussknacker validates the data type of each entry field against the OpenAPI interface definition. |
| handleErrors | If set to `true`, OpenAPI call errors are returned as a normal output value instead of failing node execution. |
| Output variable name | This variable will contain the result of OpenAPI service call  |

## Error handling output

When `handleErrors` is set to `true`, the output variable is a record with:

| Field | Description |
|-------|-------------|
| error | `true` for failed call, `false` for successful call |
| errorResponse | Error description for failed call |
| successResponse | OpenAPI response for successful call |
| statusCode | Optional HTTP status code for successful and failed calls (present when available) |

`statusCode` can be empty in some failure cases, when the underlying exception does not expose an HTTP status.
