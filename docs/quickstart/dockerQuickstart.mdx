---
title: Docker Setup Tutorial
description: Learn how to install and run the platform with Docker. Get started quickly with a local environment for workflow testing.
sidebar_label: Docker

---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import SpELFilterCondition from '@site/static/img/scenarioAuthoring/SpELFilterCondition.png';
import requestResponseExample from '@site/static/img/quickstart/requestResponseExample.png';
import streamingExample from '@site/static/img/quickstart/streamingExample.png';

You can start your journey with Nussknacker using fully-featured docker-based environment. The dependencies and manual steps were 
reduced to minimum to allows to start interacting with Nussknacker on your local machine.

## A very concise Nussknacker terminology primer

In Nussknacker, scenario describes a decision algorithm i.e. how [records](/docs/about/GLOSSARY.md#record) should be processed and what actions should be performed. Scenarios are composed of [nodes](/docs/about/GLOSSARY.md#node), which work with records. Nodes can  organize the flow, transform data carried in records, bring additional data ("enrich"), call actions in external systems, etc.

## Prerequisites

* Docker (20.10.14+) installed
* unused ports: 8080, 8181
* MacOS, Windows (10, 11), Linux

## Running 

:::info[Download and run]

<Tabs>
<TabItem value="one-liner-unix" label="MacOS & Linux One-liner script" default>

Run in the console:

```bash
curl -o- https://raw.githubusercontent.com/TouK/nussknacker-quickstart/main/download-and-start.sh | bash
```

</TabItem>
<TabItem value="one-liner-windows" label="Windows One-liner script" default>

Run in Terminal as admin (`Win+X` and then pick `Terminal (Admin)`) :

```
Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/TouK/nussknacker-quickstart/main/download-and-start.bat' -OutFile 'download-and-start.bat'; Start-Process 'cmd.exe' -ArgumentList '/c download-and-start.bat'
```

</TabItem>
<TabItem value="manual" label="Manual steps">

Do the following steps:
1. [Download](https://github.com/TouK/nussknacker-quickstart/archive/refs/heads/main.zip) Nu Quickstart 
2. Unzip it and go to the unzipped folder
3. Run `start.sh` (for MacOS or Linux) or `start.bat` (for Windows) script

</TabItem>
</Tabs>

:::

And then wait a little bit and open `http://localhost:8080/` in your favorite browser and log in as `admin:admin`

You can interact with the solution using the following user interfaces:
* [Nusskanacker Designer](http://localhost:8080) (`http://localhost:8080`)
* [AKHQ](http://localhost:8080/akhq) (`http://localhost:8080/akhq`)
* [Flink Manager](http://localhost:8080/flink) (`http://localhost:8080/flink`)

## What's in the Quickstart? 

The Quickstart bootstraps Nussknacker and its dependencies in docker containers.

<details>
  <summary>Click to read about the exposed services details</summary>

  * Nussknacker Designer - Nu scenarios are developed and managed from it
  * [Postgres](https://www.postgresql.org/) - database for the Designer
  * [Flink](https://flink.apache.org/) (Job Manager & Task Manager) - a powerful steaming processing framework
  * [Kafka](https://kafka.apache.org/) - broker for steaming data
  * [Schema Registry](https://docs.confluent.io/platform/current/schema-registry/index.html) - schema for the data in Kafka's topics
  * [AKHQ](https://akhq.io/) - UI for Kafka & Schema Registry
  * [Grafana](https://grafana.com/) & [InfluxDB](https://www.influxdata.com/) & [Telegraf](https://www.influxdata.com/time-series-platform/telegraf/) - for scenarios monitoring 
  * [Nginx](https://nginx.org/en/) - gateway for Designer and other UIs 
  * Quickstart setup - automatically creates example scenarios and generates sample Streaming and Request-Response data, creates Kafka's topics and topics' schemas.

</details>

After logging in to the Nu Designer, in the scenarios list, you will find example scenarios. They are fully functional and working. 

### Streaming example

With [`DetectLargeTransaction`](http://localhost:8080/visualization/DetectLargeTransactions) scenario we want to show you 
a [Streaming mode](/docs/about/ProcessingModes.md#streaming-mode-stateful-stream-processing) 

The scenario logic is pretty simple: it reads messages from Kafka's `Transactions` topic and 
the large ones (which amount is greater than 20), it sends them to audit (to Kafka's `ProcessedTransactions` topic).

Input data (transaction messages) are being generated by one of the Quickstart services. You can watch them in 
[AKQH](http://localhost:8080/akhq/ui/nussknacker-kafka/tail?topicId=Transactions). The scenario is already deployed and is being 
executed by the Flink engine. You can verify that by checking the [Flink Dashboard](http://localhost:8080/flink/#/job/running). 
The "large" transactions are sent to 
[`ProcessedTransactions`](http://localhost:8080/akhq/ui/nussknacker-kafka/tail?topicId=ProcessedTransactions) topic.

As a next step we'd advise you to:
1. see the metrics of the scenario - click the `metrics` button
2. see how many messages have been processed so far, and by which nodes - click the `counts` button
3. try to run Nu Tests to see how the messages are processed - click the `generated` and/or `ad-hoc` buttons (you may want to read more about the [Test functionality](/docs/scenariosAuthoring/testingAndDebugging.mdx) first)
4. try to modify the scenario - drag&drop and attach a new component from the Creator Panel and/or modify the existing scenario nodes
5. save the modified scenario and deploy the changes - click the `save` and then `deploy` button (you can go back to previous steps to verify if your changes work as expected)


<figure>
  <img
    src={streamingExample}
    alt="Docker quickstart streaming scenario example - Designer window view."
    title="Streaming scenario example"
  />
  <figcaption>
    Streaming scenario example. UI panels placement may differ from the actual one.
  </figcaption>
</figure>

### Request-Response example

The next example is [`LoanRequest`](http://localhost:8080/visualization/LoanRequest) scenario. It shows 
a [Request-Response mode](/docs/about/ProcessingModes.md#request-response-mode-instant-decision). The scenario logics describes a simple
method of determining maximum of loan amount based on loan type (passed in a `requestType` field of the input request) and a city 
(also from the request data). 

Input data (HTTP requests) are being generated by one of the Quickstart services. The scenario is already deployed and is being executed
by the Lite engine.

As a next step we'd advise you to:
1. see the metrics of the scenario - click the `metrics` button
2. see how many requests have been processed so far, and by which nodes - click the `counts` button
3. try to run Nu Tests to see how the requests are processed - click the `generated` and/or `ad-hoc` buttons (you may want to read more about the [Test functionality](/docs/scenariosAuthoring/testingAndDebugging.mdx) first)
4. you can send a request by your own, directly to the running scenario:

  <Tabs>
  <TabItem value="postman" label="Using Postman" default>

  ```
  POST http://localhost:8181/scenario/loan
  ```

  Body:
  ```
  {"customerId": "client01", "requestedAmount": 500, "requestType": "mortgage", "location": { "city": "Warszawa", "street": "Obozowa" }}
  ```

  </TabItem>
  <TabItem value="curl" label="Using curl">

  ```bash 
  curl -XPOST "http://localhost:8181/scenario/loan" -d '{"customerId": "client01", "requestedAmount": 500, "requestType": "mortgage", "location": { "city": "Warszawa", "street": "Obozowa" }}'
  ```

  </TabItem>
  </Tabs>

5. try to modify the scenario - drag&drop and attach a new component from the Creator Panel and/or modify the existing scenario nodes
6. save the modified scenario and deploy the changes - click the `save` and then `deploy` button (you can go back to previous steps to verify if your changes work as expected)

<figure>
  <img
    src={requestResponseExample}
    alt="Docker quickstart Request-Response scenario example - Designer window view."
    title="Request-Response scenario example"
  />
  <figcaption>
    Streaming scenario example. UI panels placement may differ from the actual one.
  </figcaption>
</figure>

## Cleaning up

To stop the Quickstart environment and clean the resources, just run `stop-and-clean.sh` script (you will find in the downloaded Quickstart
folder) and wait a few seconds. 