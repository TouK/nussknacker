modelConfig: {
  classPath: ["model/liteModel.jar", "components/liteBaseComponents.jar"]
  classPath: ${?MODEL_CLASS_PATH}
  kafka {
    kafkaAddress: ${?KAFKA_ADDRESS}
  }
  exceptionHandlingConfig {
    topic: ${?KAFKA_TOPIC}
  }
}