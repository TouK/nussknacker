{
  "kind": "Deployment",
  "apiVersion": "apps/v1",
  "metadata": {
  },
  "spec": {
    # we set minReadySeconds default
    "minReadySeconds": 0,
    "selector": {
    },
    "template": {
      "metadata": {
      },
      "spec": {
        "containers": [
          {
            "name": "runtime"
          }
        ],
      }
    }
  }
}
