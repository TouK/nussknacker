{
  "kind": "Deployment",
  "apiVersion": "apps/v1",
  "metadata": {
  },
  "spec": {
    # we set minReadySeconds default
    "minReadySeconds": 10,
    "selector": {
    },
    "template": {
      "metadata": {
      },
      "spec": {
        "containers": [
          {
            "name": "runtime",
            "image": "touk/nussknacker-lite-kafka-runtime"
          }
        ],
      }
    }
  }
}
