processConfigCreatorClass: "pl.touk.nussknacker.engine.management.sample.DemoProcessConfigCreator"
checkpointInterval: 10s
timeout: 10s
asyncExecutionConfig {
  bufferSize: 200
  parallelismMultiplier: 2
}

