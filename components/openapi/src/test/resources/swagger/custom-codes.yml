openapi: "3.0.0"
info:
  title: Simple API overview
  version: 1.0.0
servers:
  - url: http://dummy.io
paths:
  /code:
    get:
      parameters:
        - name: code
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: "-"
          content:
            application/json:
              schema:
                type: object
      operationId: code
