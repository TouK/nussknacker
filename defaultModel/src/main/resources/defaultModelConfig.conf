{

  #TODO: generate links to proper documentation version
  baseDocsUrl: "https://nussknacker.io/documentation/docs/scenarios_authoring/"

  componentsUiConfig: {
    filter: {
      docsUrl: ${baseDocsUrl}"BasicNodes#filter"
    }
    variable: {
      docsUrl: ${baseDocsUrl}"BasicNodes#variable"
    }
    mapVariable: {
      docsUrl: ${baseDocsUrl}"BasicNodes#mapvariable"
    }
    #needed for components docs link
    choice: {
      docsUrl: ${baseDocsUrl}"BasicNodes#choice"
    }
    #needed for node docs link
    switch: {
      docsUrl: ${baseDocsUrl}"BasicNodes#choice"
    }
    split: {
      docsUrl: ${baseDocsUrl}"BasicNodes#split"
    }
    input {
      docsUrl: ${baseDocsUrl}"Fragments#inputs"
    }
    subprocessInputDefinition {
      docsUrl: ${baseDocsUrl}"Fragments#inputs"
    }
    subprocessOutputDefinition {
      docsUrl: ${baseDocsUrl}"Fragments#outputs"
    }
  }
  componentsGroupMapping {
    hidden: null
  }
}
