import { styled } from "@mui/material/styles";

import { ComponentIcon } from "../creator/ComponentIcon";

export const NodeIcon = styled(ComponentIcon)({
    minWidth: "1.25em",
    maxWidth: "1.25em",
    minHeight: "1.25em",
    maxHeight: "1.25em",
    flexShrink: 0,
});
