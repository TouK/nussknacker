scenarioTypes {
  "streaming": {
    modelConfig: {
      liveDataPreview {
        enabled: true
        storage {
          type: "DESIGNER_DB"
          uploadIntervalInSeconds: 1
          url: "jdbc:hsqldb:file:"${storageDir}"/db;sql.syntax_ora=true"
          url: ${?DB_URL}
          user: "SA"
          user: ${?DB_USER}
          password: ""
          password: ${?DB_PASSWORD}
          schema: "public"
        }
      }
    }

    category: "Default"
  }
}
