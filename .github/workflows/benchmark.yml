name: Benchmarks

on: [push]

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run benchmarks
        shell: bash
        run: ./ciRunSbt.sh benchmarks/jmh:run -i 8 -wi 3 -f1 -t2 .SampleSpelBenchmark